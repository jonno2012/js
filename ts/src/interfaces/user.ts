export interface User {
    name: string;
    age: number;
}

// note that interfaces are basically the same as type aliases.
// the biggest difference is that you can extend interfaces using the extends keyword.